description: Document that a deployment is created based on
type: object
required:
- name
properties:
  name:
    type: string
  sequence:
    $ref: "./OrderedResources.yml"
  resourcePairings:
    $ref: "./OrderedResources.yml"
  mainScript:
    type: object
    properties:
      module:
        type: string
      function:
        type: string
